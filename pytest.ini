[pytest]
pythonpath = src
testpaths = tests
markers =
    eval: makes real Anthropic API calls against the live agent;
     run explicitly with `pytest -m eval`, not part of the default run.
asyncio_mode = auto
addopts = -m "not eval"
